export class A0028E{
    constructor(obj?){
        if(obj!=null)
        Object.keys(obj).forEach(key=>{
            this[key]=obj[key]
        })
    }
    A0028E_ID:number
    MKV9999_ID:number
    A0028_ID
}